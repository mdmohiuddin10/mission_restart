// 20% discount
const getTax = (amount, taxRate) => amount * taxRate / 100;
const add =(p, q) => p + q;

// single parameter
const getSquare = (x) => x * x
console.log(getSquare(16));

const getHalf = num => num / 5;
console.log(getHalf(800));

const firstElement = nums => nums[0];
console.log(firstElement([2, 5, 6, 8, 80]));


// annymous function
// document.getElementById('').addEventListener('click', () =>{

// })